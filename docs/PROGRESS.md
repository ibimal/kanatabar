# Progress

One line per phase: date, status, deviations from spec.

| Phase | Date | Status | Deviations |
|---|---|---|---|
| 0–10 | 2026-07-08 → 2026-07-16 | done — every [AUTO] gate green (`just gate-0`…`gate-10`); the [HW] runbook (docs/HW-TESTS.md) verified end-to-end on macOS 26.5 / kanata 1.12.0, including Run 9's local half (pkg install, reinstall-over-live, bundled notifications, wizard) | Phase-by-phase development predates the public repo; this history begins at v0.1.0. Remaining [HW]: Run 9's brew items (cask install/upgrade/uninstall against the published release). |
| v0.1.1 | 2026-07-16 | released — config/preset UX batch from first-user feedback: config.toml parse errors now surface (doctor `config file` check, never silent); `kanatactl preset add/remove` + `config reload` (no hand-editing); kanata detected in `~/.cargo/bin`/MacPorts with guided `kanata_bin` fix; empty `preset list` discovers `~/.config/kanata` configs. `just check` + gate-10 green; published + tap bumped. | kanata-bin resolution kept the §14 allowlist (root never auto-runs a user-writable path); user opts in via `kanata_bin`. config.toml auto-watch deferred — explicit `config reload` + `preset add` is safer (defaults can't hot-swap under a running child). |
| 12 | 2026-07-17 | code complete — design doc (wry-in-tao decision, shared visual tokens, panel semantics) + all three windows on one shell: **Devices…** (live hotplug refresh; nameless-device + AeroSpace-float findings fixed same-day), **Health Check…** (all 12 checks + fix hints, Copy report = §9 `--json` bundle, setup-class failures delegate to the wizard per the §11 [HARD] rule), **Setup Assistant…** (auto-opens while setup-class checks fail, ~2 s live re-check while open, "Do it for me"/pane buttons, sudo as copyable text only). §11.1 classification (`SETUP_CHECKS`/`setup_complete`) in core; page↔shell ipc validated in lib (`pages`); view-model JSON shapes pinned. `just check` + `gate-12` [AUTO] green; [HW] Runs 10–11 pending (Run 10 partially evidenced: window opens/focuses, floats in AeroSpace as dialog — `debug-windows` capture). | tao gained `rwh_06`; deny.toml crate-scoped MPL-2.0 exception for `option-ext`. Menu renames: "Devices…", "Setup Wizard…"→"Setup Assistant…", "Run Doctor"→"Health Check…". §8 "demoted once setup complete" implemented as static order (assistant above health check), not dynamic — revisit if it grates. Notification-driven fallbacks kept for webview-creation failure. |
| 12.1 | 2026-07-17 | code complete — **real TCC permission checks** (from Thaw's pattern): the doctor now reads kanatad's **own** Input Monitoring grant via `IOHIDCheckAccess` and Accessibility via `AXIsProcessTrusted` (new `ffi/tcc.rs`), replacing the always-green "TCC is unreadable" placeholder. Split into **two** checks (`input monitoring` + new `accessibility`, ALL_CHECKS 12→13) and **two** wizard steps ("Grant Input Monitoring" / "Grant Accessibility", each opening its own pane). `just check` + `gate-12` [AUTO] green. | **Deviation from the prior SPEC [HARD] "grants cannot be read programmatically"** — that conflated the SIP-protected TCC.db (truly unreadable) with the public per-process access APIs (readable for the *calling* process); SPEC §2 amended. Conservative pending HW: only a definitive Input-Monitoring `Denied` fails; `Unknown`/untrusted stay informational-green so a system-context daemon quirk can't false-red a working setup. **HW-pending (ledger #19):** confirm a granted root LaunchDaemon reads its own grant accurately, then flip not-trusted → red. |
| 12.1-hw | 2026-07-17 | **ledger #19 READ CONFIRMED on HW** (installed root LaunchDaemon, macOS 26.5): ungranted → `denied`/not-trusted, granted (after remove+re-add **and daemon restart**) → `granted (verified via …)`. **Policy flipped:** not-granted (Denied *and* Unknown / not-trusted) now **fails** the check — full red/green, no more hedge. Test/CI daemons set `KANATABAR_SKIP_PERMISSION_CHECK` (mirrors `--skip-driver-check`) since they can't hold a real grant. `just check` + `gate-12` green. | **Finding: grants take effect only after a daemon restart** (a process caches its launch-time TCC decision) — re-granting alone kept reading `denied` until `launchctl kickstart -k`. Consequence: the "Set it up for me" flow must restart kanatad after the user toggles the pane — **follow-up**, not yet wired. The system-context `IOHIDRequestAccess`/`AXIsProcessTrustedWithOptions` registration is also still unverified separately. |
| 12.2-hw | 2026-07-18 | **fresh-child TCC probe CONFIRMED on HW** (ledger #19): doctor's spawned `kanatad tcc-status` child is evaluated afresh by TCC — Accessibility toggle-off read ❌ within seconds, toggle-on ✅ within seconds, same daemon pid, zero restarts. Doctor/wizard now have **live** grant status (Thaw's poll-until-green UX) from the root LaunchDaemon. Thaw source re-checked first: it uses NO Input Monitoring (Accessibility + Screen Recording only) and no restart logic — its liveness is purely GUI-session `AXIsProcessTrustedWithOptions` polling, which is what the fresh-child probe replicates for a daemon. | Refines 12.1-hw: the launch-cached verdict is per-*process*, not per-identity — restart is NOT needed for status anymore. **Same-day follow-through (user flagged IM was untested — correct):** IM toggle test ran too — live ❌/✅ via fresh probe confirmed for `IOHIDCheckAccess`; revocation kills neither process (running kanata keeps working on its spawn verdict → doctor-red+Running is truthful); **#19a CONFIRMED** — revoke → restart → `Degraded{InputMonitoringDenied}` → re-grant → plain `kanatactl restart` → `Running`, zero daemon restarts across the cycle. Unlocks supervisor auto-recovery (retry-on-grant while degraded). Still unverified: system-context request registration ("Set it up for me"). |
| 12.4 | 2026-07-18 | code complete — **supervisor auto-recovery (retry-on-grant)**: while `Degraded{InputMonitoringDenied}`, a `grant_timer` select arm polls the fresh-child TCC probe every 3 s and dispatches Restart the moment BOTH Input Monitoring and Accessibility read granted — the deny→grant→working loop self-heals with zero user commands and zero daemon restarts (kanata is respawned; kanatad never restarts). `describe()` now tells the user recovery is automatic. `just check` + `gate-12` green; [HW] pending: one deploy-and-regrant cycle (doubles as the next deploy — see HW-TESTS #19). | **Scoped amendment to the supervisor's zero-polling [HARD] rule** (recorded in the module doc + SPEC §2/§6.5): polling runs *only* while degraded-on-denial, because macOS provides no TCC-change notification API (even GUI apps poll; Thaw ticks every 3 s). The old no-retry rationale ("retrying is futile") is preserved in spirit: the watch retries exactly once, exactly when the grant appears — never blind. |
| 12.3 | 2026-07-18 | code complete — **shell completions for kanatactl** (bash/zsh/fish via clap_complete): new `kanatactl completions <shell>` subcommand (prints to stdout, pipe-safe), and `install` (daemon component) drops the scripts into each shell's standard lookup dir — zsh `/usr/local/share/zsh/site-functions/_kanatactl` (in Apple zsh's default fpath), fish `/usr/local/share/fish/vendor_completions.d/` (default XDG_DATA_DIRS), bash `/usr/local/etc/bash_completion.d/` (needs bash-completion pkg). `uninstall` removes them; gate-6 leave-nothing audit extended to cover them. `just check` green. | Not in SPEC (§9 doesn't mention completions) — additive CLI UX, no protocol/behavior change. Completion install is best-effort: a failure warns but never fails `install`. |
| 12.2 | 2026-07-17 | code complete — **request-access ("Set it up for me")**, Thaw's second half: the two grant steps gain a button that asks the **daemon** (new `RequestPermission{kind}` IPC) to call `IOHIDRequestAccess` / `AXIsProcessTrustedWithOptions` for itself, registering kanatad's Privacy-pane entry, then opens the pane and re-checks (~2 s). Page action `request:<index>`, validated against the static step table like `run`/`open`. `just check` + `gate-12` [AUTO] green. | The request **must** run in kanatad, not the GUI tray: TCC attributes a grant to the *calling* process, and kanatad is the responsible process for kanata's device access — a tray-side request would grant the useless tray entry. Whether a system-context daemon's request actually registers the entry (vs. needing a GUI session) is **HW-pending (ledger #19)**; the manual +/Cmd+Shift+G path stays in the instruction as fallback. |
